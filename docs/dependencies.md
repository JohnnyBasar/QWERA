# Dependencies

The QWERA toolbox uses:
- numpy
- pandas
- matplotlib
- wetterdienst
- QGIS core providers: native, gdal

All required dependencies are bundled with the plugin ZIP.
Internet access is required only for DWD data retrieval.
